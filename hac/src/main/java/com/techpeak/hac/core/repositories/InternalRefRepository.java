package com.techpeak.hac.core.repositories;

import com.techpeak.hac.core.models.InternalRef;
import org.springframework.data.jpa.repository.JpaRepository;

public interface InternalRefRepository extends JpaRepository<InternalRef, Long> {
}
